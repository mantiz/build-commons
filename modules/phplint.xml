<?xml version="1.0" encoding="UTF-8"?>
<project>
    <target name="phplint" extensionOf="-test-static:main~hook">
        <exec executable="${bindir}/parallel-lint" failonerror="${phplint.fail-on-violation}" dir="${basedir}">
            <arg value="${srcdir}" />
        </exec>
    </target>
</project>
